{% extends 'base.html.twig' %}

{% block title %}New Book{% endblock %}

{% block body %}
    <h1>Create new Book</h1>

    {{ include('book/_form.html.twig') }}

    <a class="btn btn-primary" type="button" href="{{ path('book_index') }}">Back to list</a>
{% endblock %}
